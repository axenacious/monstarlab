server:
  version: v0.1
  addr: :8080
  mode: debug
  static_dir: ./public
  view_dir: ./view
  upload_dir: ./storage
  max_multipart_memory: 10

database:
  datasource: "host=0.0.0.0 user=postgres password=mysecretpassword dbname=postgres host=0.0.0.0 port=5432 sslmode=disable"
